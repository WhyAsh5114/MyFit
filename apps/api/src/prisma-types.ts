// Re-export all Prisma types for external consumers (e.g., the frontend)
// The generated client.ts has @ts-nocheck which prevents direct consumption via package exports
export * from "../generated/prisma/client.js";
